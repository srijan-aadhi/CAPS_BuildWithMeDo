{
  "name": "caps-extension",
  "displayName": "CAPS",
  "description": "Context-Aware Prompt Snippets",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.119.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "caps-extension.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "caps.setSnippetsApiUrl",
        "title": "CAPS: Set snippets API URL (Deno / GET JSON)"
      },
      {
        "command": "caps.setSnippetsApiBearer",
        "title": "CAPS: Set snippets API Bearer (optional)"
      },
      {
        "command": "caps.setDirectSupabaseUrl",
        "title": "CAPS: Set direct Supabase REST URL (fallback)"
      },
      {
        "command": "caps.setDirectApiKey",
        "title": "CAPS: Set direct Supabase API key (fallback)"
      },
      {
        "command": "caps.setSupabaseAnonKey",
        "title": "CAPS: Set Supabase anon key"
      },
      {
        "command": "caps.setAnthropicApiKey",
        "title": "CAPS: Set Anthropic API key"
      },
      {
        "command": "caps.pickPrompt",
        "title": "CAPS: Pick Prompt"
      },
      {
        "command": "caps.boostPrompt",
        "title": "CAPS: Boost Prompt",
        "icon": "$(sparkle)"
      },
      {
        "command": "caps.clearCapsSecrets",
        "title": "CAPS: Clear all stored secrets"
      }
    ],
    "chatParticipants": [
      {
        "id": "caps.assistant",
        "name": "caps",
        "description": "Boost a rough prompt with Anthropic AI before sending to inline chat",
        "isSticky": false
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "caps.pickPrompt",
          "group": "caps@1"
        },
        {
          "command": "caps.boostPrompt",
          "when": "editorHasSelection",
          "group": "caps@2"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.118.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "axios": "^1.16.0"
  }
}
